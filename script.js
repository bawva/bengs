// Simple script to handle the fade-in animations
document.addEventListener("DOMContentLoaded", () => {
  // Add a class to body when page is loaded to trigger animations
  document.body.classList.add("loaded")

  // Optional: Add some interaction or additional animations here
  console.log("Bengs family site loaded successfully!")
})
